const config = require('./config')
const api = require('./api')

module.exports = {
    apiRootUrl: false,
    name: "mk-service-auth",
    version: "",
    description: "",
    author: "lsg",
    config,
    api,
}